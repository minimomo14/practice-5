import Job from "./Job";
import Candidate from "./Candidate";

function findQualifiedCandidates(job:Job, candidate:Candidate): Array {
    // find the candidate.filter(); to return jobFits

}
export default findQualifiedCandidates;

